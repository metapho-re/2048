export const bodyElement = document.querySelector("body") as HTMLBodyElement;
export const buttonElement = document.querySelector(
  "button",
) as HTMLButtonElement;
export const gridElement = document.querySelector("#grid") as HTMLDivElement;
export const scoreElement = document.querySelector(
  "#score",
) as HTMLParagraphElement;
export const statusElement = document.querySelector(
  "#status",
) as HTMLParagraphElement;
